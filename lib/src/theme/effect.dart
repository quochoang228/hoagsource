import 'package:flutter/material.dart';

abstract class BaseEffect {
  static const BoxShadow shadowNavBarEffect0 = BoxShadow(
    color: Color(0x0c000000),
    offset: Offset(0, 0.75),
    blurRadius: 0,
    spreadRadius: 0,
  );

  static const BoxShadow shadowNavBarEffect1 = BoxShadow(
    color: Color(0x0c000000),
    offset: Offset(0, 2),
    blurRadius: 12,
    spreadRadius: 0,
  );

  static const BoxShadow shadowNavigationBarEffect0 = BoxShadow(
    color: Color(0x14000000),
    offset: Offset(0, -2),
    blurRadius: 4,
    spreadRadius: 0,
  );

  static const BoxShadow shadowNavigationBarEffect1 = BoxShadow(
    color: Color(0x0c000000),
    offset: Offset(0, -0.75),
    blurRadius: 0,
    spreadRadius: 0,
  );

  static const BoxShadow shadowNavigationBarEffect2 = BoxShadow(
    color: Color(0x0c000000),
    offset: Offset(0, -2),
    blurRadius: 12,
    spreadRadius: 0,
  );

  static const BoxShadow cardShadowEffect0 = BoxShadow(
    color: Color(0x14000000),
    offset: Offset(0, 0),
    blurRadius: 12,
    spreadRadius: 0,
  );

  static const BoxShadow dividerTopEffect0 = BoxShadow(
    color: Color(0xffe4e5e7),
    offset: Offset(0, 1),
    blurRadius: 0,
    spreadRadius: 0,
  );

  static const BoxShadow dividerBottomEffect0 = BoxShadow(
    color: Color(0xffe1e3e5),
    offset: Offset(0, -1),
    blurRadius: 0,
    spreadRadius: 0,
  );

}